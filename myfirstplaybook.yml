--- 
- hosts: all
  become: yes
  tasks:
  - name: i want install webserver only on ubuntu nodes
    apt:
      name: git
      state: present
    when:  ansible_os_family":"Debian"




  - name: i want install webserver only on ubuntu nodes
    apt:
      name: git
      state: stared
    when:  ansible_os_family":"Debian"